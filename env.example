# Database
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Secret (cambia questo con una stringa casuale molto lunga)
JWT_SECRET=cambia-questo-con-qualcosa-di-super-sicuro-e-lungo

# Server
PORT=3000
NODE_ENV=development

# Frontend URL (il tuo dominio Railway o custom)
FRONTEND_URL=http://localhost:3000

# App URL (per link nelle email)
APP_URL=https://tieralba-backend-production-f18f.up.railway.app

# Admin Key (per gestire i signals)
ADMIN_KEY=cambia-con-una-chiave-admin-sicura

# MetaApi (per connessione broker MT4/MT5)
METAAPI_TOKEN=

# Stripe (per pagamenti - opzionale)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_PRICE_IDS=

# Resend (per email - opzionale)
RESEND_API_KEY=
EMAIL_FROM=TierAlba <onboarding@resend.dev>
